{
	"name": "blixter",
	"private": true,
	"workspaces": [
		"packages/*",
		"apps/*",
		"scripts/"
	],
	"scripts": {
		"dev": "bun task-runner.ts dev",
		"test": "bun task-runner.ts test",
		"build": "bun task-runner.ts build",
		"reset": "bun task-runner.ts reset",
		"preview": "bun task-runner.ts preview",
		"secrets": "bun task-runner.ts secrets"
	},
	"devDependencies": {
		"@types/bun": "1.1.3",
		"nodemon": "^3.0.1",
		"prettier": "^3.0.3",
		"prettier-plugin-svelte": "^3.0.3",
		"prettier-plugin-tailwindcss": "^0.5.3",
		"typescript": "^5.1.6"
	},
	"trustedDependencies": [
		"@sveltejs/kit",
		"esbuild",
		"svelte-preprocess"
	]
}
